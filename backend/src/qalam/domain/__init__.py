"""Domain layer: entities, value objects, ports, and orchestration.

Pure Python. No web framework, no database driver, no model runtime. Anything
in here must be importable from a training notebook or an offline batch job
without dragging in a server.
"""
